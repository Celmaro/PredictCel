[build]
builder = "RAILPACK"
buildCommand = "pip install -e .[dev] && python -m pytest"

[deploy]
startCommand = "python -m predictcel.railway"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
